%------------------------------------------------------------------------
% GOAL State
%------------------------------------------------------------------------
goal(I) :- holds(planningHasComponent(test_tree),I),  step(I).
goal(I) :- holds(planningHasComponent(cdao_species_tree),I),  step(I).
%------------------------------------------------------------------------
